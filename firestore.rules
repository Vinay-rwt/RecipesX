rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all reads/writes in development (will be locked down in Phase 7)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
