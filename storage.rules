rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Recipe photos: public read, authenticated write/delete
    match /recipes/{recipeId}/photos/{photo} {
      allow read: if true;
      allow write: if request.auth != null;
      allow delete: if request.auth != null;
    }
  }
}
